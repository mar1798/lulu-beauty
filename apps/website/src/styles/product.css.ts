import { style } from '@vanilla-extract/css'
import { color } from 'widgets/styling/lib'

/**
 * Залитое сердце в кнопке «в избранном» на странице товара.
 *
 * Красим только иконку, а не всю кнопку: она вторичная рядом с «в корзину»,
 * и заливать её маркой целиком значило бы спорить с главным действием. Цвет
 * при этом тот же акцентный — состояние узнаётся с той же карточки каталога,
 * где сохранённое сердце залито розовым.
 */
export const savedHeart = style({
  color: color.brand('600'),
})
