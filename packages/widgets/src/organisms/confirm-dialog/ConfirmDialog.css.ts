import { style } from '@vanilla-extract/css'

/**
 * Собственных размеров у подтверждения нет — их задаёт `Modal size="sm"`.
 * Класс остаётся точкой для точечных правок с места использования.
 */
export const container = style({})
