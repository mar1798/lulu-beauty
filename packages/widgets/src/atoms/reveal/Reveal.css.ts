import { style } from '@vanilla-extract/css'

/**
 * Обёртка появления сама по себе невидима — стилей у неё нет. Класс оставлен,
 * чтобы вызывающие могли докинуть свои (тег `li` в списках, ширина в сетках).
 */
export const container = style({})
