node_modules/
.vscode/
.idea/
coverage/

### macOS template
# General
.DS_Store
.AppleDouble
.LSOverride

# Local
# Agent configuration: skills, slash commands, settings and worktrees are
# per-machine, not part of the project.
.claude/
.env
# Every copy beside it too: `deploy/release.sh` refuses to deploy a dirty
# working tree, and a backup taken before editing production variables is
# otherwise exactly that.
.env.prod*
dist
.turbo

# Python (apps/api)
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
apps/api/uploads/
